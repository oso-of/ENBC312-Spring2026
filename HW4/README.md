##Question 1
In the first problem, I created a Car class that stores the brand and year as private variables. I implemented public setter functions to modify these values and getter functions so they could be accessed outside the class. 
In the main() function, I created an object representing a Toyota from the year 2020 and printed the stored values using the getter functions. 

##Question 2
In the second problem, I implemented a Book class with the private data members title and pages. Similar to the first problem, I used setter functions to assign values and getter functions to retrieve them. In the main() 
function, I created an object representing the book The Alchemist and displayed its title and page count. 

##Question 3
In the third problem, I explored inheritance by creating a base class called Animal that stores the species name. I then created a derived class called Dog that inherits from Animal and adds an additional variable for breed. 
The derived class uses the base class constructor to initialize the species while also setting its own breed variable. In the main() function, I created a Dog object with the species set to "Mammal" and the breed set to 
"Labrador," and printed both values. 
